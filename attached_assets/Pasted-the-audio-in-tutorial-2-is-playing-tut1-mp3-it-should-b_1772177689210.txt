the audio in tutorial 2 is playing tut1.mp3 it should be tut2.mp3
the audio in tutorial 6 is playing tut5.mp3 it should be tut6.mp3
the audio in tutorial 9 is playing tut8.mp3 it should be tut9.mp3
the audio in tutorial 13 is playing tut12.mp3 it should be tut13.mp3
You are a senior full-stack engineer. Your task is to examine the existing JOAP Supplier Management System repository (TypeScript/Node/React stack) and ensure it fully matches the PDF requirements. The current implementation includes hashing-based lookups, trie search, FIFO inventory deduction, ARIMA‑like forecasts, bcrypt password hashing, inventory/orders/billing/accounting modules, and basic UI pages. Do not remove or break existing features, but expand and refine them.

Key missing or incomplete features to implement:

Reversing Entries in Accounting

Implement routes under /api/accounting/reversals that allow an admin to select existing ledger entries and create reversing entries (same amounts but opposite debit/credit).

Update the database schema if necessary to link a reversing entry to the original.

Add an admin UI in the Accounting module: show a list of ledger entries with “Reverse” buttons; on click, confirm and create a reversing entry (append‑only).

Update the ledger summary and charts to include reversing entries in totals.

Forgot Password Flow

Add endpoints to generate reset tokens (/api/auth/forgot-password) and verify/reset passwords (/api/auth/reset-password?token=...).

Store a random token and expiry in the User document; invalidate after use.

Add a client page for requesting a reset (enter email/username) and a page for entering a new password using the token. Provide form validation and success/failure toasts.

When the password is reset, clear the token fields.

Real‑Time Dashboard and Charts

Expand Socket.IO events to emit ORDER_CREATED, ORDER_PAID, ORDER_RELEASED, INVENTORY_LOG_CREATED, and LEDGER_POSTED.

Modify the client dashboard to subscribe to these events and automatically update KPI cards and charts without refreshing the page.

Convert existing static charts to dynamic ones using live data; for example, update revenue and inventory charts when a payment or inventory log event is emitted.

Partial Payments Support

Update the billing logic to allow multiple payments per order. Track amountPaid per order in the database.

Add a column and UI elements to show partial vs full payment status. Only set the order status to “Paid” or “Pending Release” once amountPaid >= totalCost.

Provide an API endpoint and client form to log additional payments (with GCash number and reference) until the order is fully paid.

Inventory and Accounting Adjusting/Reversing UI

For inventory, expose the adjusting-entry modal clearly on the inventory page and ensure staff can create adjusting logs (positive or negative) with reasons; reflect adjustments in quantity and alerts.

For accounting, include a dedicated “Reversing Entries” tab or button in the Accounting page.

Global Search UI Enhancements

Enhance the search bar to display results grouped by entity type (Items, Orders, Customers) with prefix suggestions from the trie.

Provide clickable suggestions that navigate to the relevant record.

User Password Reset Page

Ensure there is a client page where users can set a new password using the token sent by email (or displayed for demo purposes).

Add this page to the auth flow and route protection logic.

Reports Module Completion

Integrate the forecasting routes into a full Reports section with filters for date range, metrics (inventory, sales, finance), and export options.

Display forecast charts using the ARIMA (or simplified ARIMA) results from the backend.

Add buttons to export to CSV and PDF from the UI.

Maintenance and Settings UI

Provide a front-end page for backup and restore operations (Admin only). Allow downloading a JSON backup and uploading a JSON file to restore; show logs of backup/restore actions.

Expose the Settings module (company name, reorder threshold, theme) in the UI with save buttons that persist to MongoDB.

Help and About Pages Refinement

Confirm that the Help page includes FAQs, a feedback form that writes to SystemLog or a dedicated collection, and an admin message system.

Ensure the About page shows current version, tech stack, and developer credit “JOAP HARDWARE”.

Search and Indexing Consistency

Make sure that hashing-based lookup and trie-based prefix search are both used by the search API. Document in code comments how hashing and the trie work together.

Rebuild the trie whenever relevant data changes (items, customers, orders) and ensure indexes are created in MongoDB.

Documentation and Comments

Update README and code comments to explain how the new reversing, password-reset, real-time updates, partial payments, and forecasting features are implemented.

Summarize for future developers how the system satisfies the PDF requirements.

After completing all tasks, output a clear summary describing:

Which algorithms (hashing, trie, FIFO, ARIMA, bcrypt) are now integrated and where.

New endpoints, models, or UI pages created.

Changes to existing files.

How real-time charts, partial payments, reversing entries, and password reset flows function.

Do not break existing functionality; extend safely and ensure the app builds and runs.